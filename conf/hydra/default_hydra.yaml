sweep:
  dir: ${runs_basedir}/${hydra.job.name}/multirun/${now:%Y-%m-%d_%H-%M-%S}
  subdir: ${hydra.job.num}
run:
  dir: ${runs_basedir}/${hydra.job.name}/singlerun/${now:%Y-%m-%d_%H-%M-%S}
job:
  config:
    override_dirname:
      exclude_keys:
        - seed
        - runs_basedir
        - data_basedir